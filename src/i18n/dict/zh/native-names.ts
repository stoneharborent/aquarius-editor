// CJK-script native language names ("autonyms") used by the caption language pickers and by the
// translation targets the agent accepts. They are language identifiers written in their own
// script, not English copy that could be translated, so they live with the locale data rather
// than in the (English-source) caption components.
export const JAPANESE_NATIVE_NAME = '日本語';
export const KOREAN_NATIVE_NAME = '한국어';
